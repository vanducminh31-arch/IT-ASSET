// UI rendering functions (skeleton)
import { TX, STOCK, STORES, OFFICES, WAREHOUSES, DEVICES } from "./state.js";

export function renderDashboard() {
  // Render stats, recent transactions, top stock items
}

export function renderTransactions() {
  // Render transactions table, filter, pagination
}

export function renderStock() {
  // Render stock table, filter, edit
}

export function renderStores() {
  // Render stores table, devices assigned
}

export function renderOffices() {
  // Render offices table, devices assigned
}

export function renderWarehouses() {
  // Render warehouses table, devices stored
}
